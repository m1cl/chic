use crate::AuthManager;

pub async fn get_playlists() {
  // let token = AuthManager::get_access_token().await;
  println!("yeah we get the playlists");
  // match token {
  //   Ok(t) => println!("the fu token {}", t),
  //   Err(err) => println!("ERROR: \t {}", err),
  // };
}
